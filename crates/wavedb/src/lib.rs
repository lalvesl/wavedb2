//! `wavedb` — the user-facing client library (`Db` handle, typed CRUD, server-fn
//! call bindings).
//!
//! Implementation is staged bottom-up; this crate is a placeholder so the
//! workspace resolves while `wavedb-core` and the engine are built first. See
//! `crates/wavedb/README.md` for the target surface.
