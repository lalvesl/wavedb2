several genuinely unresolved things. Grouped by when they block you:

Blocks finishing wavedb-core (next slice)

1. STRUCT_HASH algorithm. Must be a fixed, deterministic hash — DefaultHasher varies by compiler/run, can't use it. Pick one
   (FNV-1a / xxHash / truncated BLAKE3) and pin it as a const.
   Reply = About the hash use ahash crate with const seed, use random to generate const seed ([u64;4]) and write in the first page of data.bin this makes best performance for hash u128 and security.

2. Also: trunc8 gives only 256 co-location buckets — many NonUnique types
   per tenant collide on that discriminator. Is that fine (it's only a hint, full hash disambiguates) or a problem?
   Reply = Yes you are right, i thinked about method to reduce
   probability of hash colision (in storage on disk is not a problem, but when crate a KV cache in memory that's a problem) but if
   use 15 bits only with random data or in the future make a MASK of this u15 for each user plugged, this already protected by u48
   of tenant then REMOVE THIS U8 OF STRUCT_HASH, make more simple.

3. Wire gaps
   single allocation but the impl does 2 (stack+heap then concat) — keep the claim or fix.
   Reply = This is recursive, when there's "unknow" type provide special struct like Metadata, metada also need Wire to call STACK_SIZE to sum in stack, with this that's possible to create one memmory allocation of Vec<U8>

4. Evolution model implication. No migration chains means: to read old data after a shape change, you must keep the old struct
   declared (its own STRUCT_HASH) + write first_try to convert. So the schema crate accumulates old struct versions. Confirm that's
   the intent.
   Reply = Yes that's correct this is the part of lazymigrations,
   imagine an application with millions of users and i need to execute extreme havy load of DB to migrate schema and the rollback
   need another time of havy load in DB makking slow transactions to block all users to use this is senario in SQL DB, please
   create another md file to compare common SQL to this DB and add this topic is advantage. About the necessity to keep old code, in the future plain is create feature flag, for server keep this, but the client keep only necessary structs to no grow so much WASM code, but in backend this will cost less than 1MB.

Blocks build.rs registry

4. Scanner scope. Does generate_registry() scan only the schema crate's own src/? Then #[wavedb] structs from dependency crates,
   cfg-gated, or macro-generated ones are invisible. Real limit of build-scan vs proc-macro — define the boundary.
   Reply = that's both imagine when some struct use wavedb proc-macro also implements the necessary derivations then functions and types like PivotId Pivot already defined, then build.rs can generate a module to call this thing, imagine build.rs generate the match with the some_module::SomeStructWithWaveDbMacro::STRUCT_HASH
   after this need only to "call" this module in src include!(concat!(env!("OUT_DIR"), "/foo.rs")), about the src yes only in to src folder

Blocks storage (M2)

5. BpTree on disk. B+tree nodes are themselves hash-addressed pages; inter-node pointers are Ids ⇒ navigation = random get per
   node. Define the node format + how "ordered by CREATED_AT" works over hash-addressed storage.
6. Pivot lifecycle. When is a Pivot created, who owns it, one-per-collection-field-per-tenant? Nested recursion depth.
7. Multi-write atomicity. Insert = write record + update BpTree + bump Pivot.counter = 3 writes. Journaled as one unit?
   Transaction boundary for a server fn doing several writes?
8. Time source. CREATED_AT ns — clock skew / monotonicity / NTP-backwards. Single-writer-per-tenant helps; SALT breaks ties.
   Define the guarantee.

Blocks nodes (M3+)

9. Cross-tenant read path. User of tenant B reads tenant A's data (with grant) — which node serves it, where is the permission
   checked?
10. Replication durability window. Async replication ⇒ owner crash before replicating loses unreplicated writes (durability =
    owner journal only). Acceptable?
11. Unbounded history. Timeline is first-class + slow-node deferred ⇒ hot tier keeps all history forever. Space story?
